<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <item
        android:id="@+id/center"
        android:title="center on"
        app:showAsAction="never"/>

    <item android:id="@+id/pin"
        android:title="pin on center"
        app:showAsAction="never"/>

    <item android:id="@+id/alpha"
        android:title="alpha f"
        app:showAsAction="never"/>

    <item android:id="@+id/circle"
        android:title="circle"
        app:showAsAction="never"/>
    <item android:id="@+id/stupid"
        android:title="stupid"
        app:showAsAction="never"/>

</menu>